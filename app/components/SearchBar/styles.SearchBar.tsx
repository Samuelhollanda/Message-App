import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    search: {
        borderWidth: 0.5,
        borderRadius: 20,
        padding: 10,
        margin: 10,
    },
})

export default styles;